### 需求说明   

1. 需要上传的文件   
系统需要上传template里面的excel明细列表。  
同时根据项目类型（科研项目、非科研项目）来提交一个劳务费明细的PDF（见Word模板）。  


2. 需要能创建或者维护固定清单   
由于研究生、本科生、AI补充是分开发的，并且也是动态变化。  
因此需要支持维护一个固定人员清单（参见template里面excel）。用目前的基于excel上传、复制应该就行了   

### 功能提升  

3. 人员录入不能是一次性的：依次录入之后，未删除的人员要能一直操作（编辑、重新分组、反复发放）。

4. 分组要能真正用起来：
   - 导入名单时**按分组批量导入**（一份名单整批进某一组）
   - 勾选金额时**按分组勾选**（整组全选、整组统一设金额）
   - **同一个人不能出现在不同分组里**（避免重复发放）

---

### 实现情况

#### 1. 上传 Excel + 按项目类型出 PDF —— 已完成

- 第 1 步新增**项目类型**选择（科研经费 / 非科研经费），决定套哪份 Word 底板：
  - 科研经费：`templates/武汉大学学生劳务费发放明细表（科研经费）.docx`，明细列含**发放事由及依据**
  - 非科研经费：`templates/武汉大学学生劳务费发放明细表（非科研经费）.docx`，明细列含**所在学院 / 标准 / 工时**
- 第 3 步新增**系统上传 Excel**：严格按学校系统要求的三列
  `学号 | 姓名 | 助研津贴(三兼费)`，只含本次勾选的人（缺学号会拦下来并给中文提示）。
  模板取自 `templates/助研费用发放列表.xls`。
- 新增 **PDF 明细表**导出：由 `pdf_gen.py` 读取刚生成的 docx（文字、表格网格、合并单元格、
  字号、页面尺寸与边距）用 reportlab 自己排版，**不需要装 Word / LibreOffice**。
  两类模板都验证过单页与跨页（约 35 人）样例。
- 导出按钮：Word 明细表 / PDF 明细表 / 系统上传 Excel / 名单 Excel / 全部导出。

#### 2. 固定清单与分组 —— 已完成

- 新增 `people_store.py`（SQLite）：人员、分组、本次名单存在 `app/people.sqlite3`，
  每次修改在**同一个事务**里提交，并带 `revision` 乐观锁。
- 预置三个固定清单：**研究生 / 本科生 / AI 补充**；可自行**新增、改名、删除分组**。
  删除分组**不会删除组内人员**，只解除归属。
- **一人可属于多个分组**（例如同时是「研究生」和「博士助研」）。
- 人员库界面（`web/library.js`）支持：搜索、编辑、删除人员、按分组筛选、
  「将当前列表加入本次」、**备份人员库 / 导入备份**（JSON）。
- 名单导入（Excel / 粘贴）会自动并入人员库，按学号或姓名匹配，不会产生重复。
- 旧版 `app/settings.json` 里的名单会在首次启动时**自动迁移一次**，原文件保留。

#### 3. 人员不是一次性的 —— 已完成

- 人员录一次长期保存：关浏览器、重启服务、换月份、套用模板、清空本次名单都不会丢。
- 只有明确点「删除人员」才移出人员库；删除是**软删除**，
  过期页面的保存无法把已删除的人复活，也不会影响到其他人。
- 「保存并继续添加」可连续录入，前一位已经存好，不会被后一位覆盖。
- 从本次名单移出（行尾 `×`、「将未勾选人员移出本次」、「清空本次名单」）
  **只影响本次发放，人员档案保留**。

#### 4. 分组能真正用起来 —— 已完成

分组分成**两层**，因为这两件事的规则本来就不一样：

| | 身份组 `kind="identity"` | 项目组 `kind="project"` |
| --- | --- | --- |
| 例子 | 研究生 / 本科生 / AI 补充 | 国家重点研发 / 横向课题 A |
| 数量 | 每人**有且只有一个** | 一人可属于多个 |
| 用途 | 导入名单、按组勾选发放都按它 | 筛选和批量勾选 |
| 为什么 | 同一个人不可能既是研究生又是本科生，**从根上杜绝重复发放** | 一个人确实会同时参与多个项目 |

- **按分组批量导入**：第 1 步新增「导入到分组」下拉（列出全部身份组），
  选中后整份名单一次性进该组（`/api/groups/roster`，单事务写入）。
  新接口 `group.roster` 支持 `replace`：本组这次没再出现的人**保留档案**，只是脱离该组。
- **跨组查重**：导入身份组时先整批校验，同一个人已在别的身份组就把**整份导入拦下来**
  并说明是谁、在哪一组，不会静默改归属、也不会产生半截数据。
- **按分组勾选**：第 2 步表格按身份组分段，每组一行组头
  （`全选本组` / `全不选本组` / `给本组设金额…`），表格上方另有一排分组按钮，
  点一下即整组全选/全不选。金额按钮配合分组 = 该组统一金额。
- 第 2 步表格新增「身份组」列，可直接下拉改归属，改完自动归到新组下面。
- 人员编辑器：身份组是**单选下拉**，项目组是**复选框**。
- 旧数据自动归位：早期的 `groupIds` 多选关系会在首次打开时迁移——
  第一个身份组当身份，其余自建分组当项目组。

#### 5. 事由不再写"xxx等"，改成可填模板 —— 已完成

- 默认模板：`{身份}{姓名}参与了【待填写项目名称】项目，完成了【待填写工作内容】工作。`
  - `{身份}` 换成身份组名（研究生 / 本科生 / AI 补充）
  - `【】` 是"待填写"标记：导出时**去掉方括号 + 加黄色高亮**（Word 的 `w:highlight`，
    PDF 里是黄底），提醒到文件里补写；填了值就直接写值、不高亮
- **按项目组发放时项目名统一填**：抽屉里选「事由用哪个项目组」，
  这一组的人事由里统一写该组名。不选则保留高亮占位——
  刻意**不拿表头的项目名称顶替**，因为表头那个是整张表的项目，
  不一定等于某个人实际参与的项目，写错了比留空更危险
- 某个人自己填过事由的，仍然以他自己写的为准（优先级最高）
- 「发放事项说明」那一段也改成不点名、不带人数：
  `本人项目组本科生、研究生在{时段}参与科研项目…的研究。…各人的参与项目和具体工作见下表。`
  带占位符或用户自己写的说明照旧原样保留
- **不动已有数据**：新设置项只在用户主动填写后才写库；
  旧数据里没有 `reasonTemplate` 时由 `docx_gen.DEFAULT_REASON_TEMPLATE` 兜底，
  打开就是新行为，不需要迁移，也不改写任何已有记录

#### 6. 页面数字显示与导出完全对齐 —— 已完成

- 金额统一按「分」四舍五入（`ROUND_HALF_UP`）：
  前端 `roundMoney`、后端 `to_cents` 同一口径，
  所以 `100.5 × 3` 是 `301.5`，不会再出现 `301.49999999999994`
- 明细表合计改为按**整数分累加**再还原，避免浮点误差让合计和明细对不上
- Word / PDF 表格里的金额与标准加千分位（`1,200`、`2,000`），
  与页面上的显示一致；Excel 名单的金额列用 `#,##0.##` 格式，
  单元格里仍是数字，导回来不影响计算
- 提交给学校系统的三列表**不加**千分位（要纯数字）

---

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/library` | 人员库（分组 + 人员，分组带 `kind`） |
| GET | `/api/library/backup` | 导出人员库备份 JSON |
| POST | `/api/people/save` | 新增 / 编辑人员（`identityId` 一个 + `projectIds` 多个） |
| POST | `/api/people/delete` | 删除人员（软删除） |
| POST | `/api/people/bulk-save` | 整批保存人员（一次事务） |
| POST | `/api/people/bulk-delete` | 整批删除人员 |
| POST | `/api/groups/save` | 新增 / 重命名分组（`kind` 决定身份组还是项目组） |
| POST | `/api/groups/delete` | 删除分组（保留组内人员，只解除归属） |
| POST | `/api/groups/roster` | **按分组批量导入名单**，`replace` 可做替换式导入 |
| POST | `/api/library/import` | 导入人员库备份 |
| POST | `/api/export` | `kinds` 支持 `docx` / `pdf` / `xls` / `submission` |

所有写接口都要带读取时返回的 `revision`，不一致会返回 **409** 和中文提示，避免两个页面互相覆盖。

---

### 验收方式

```bash
python3 -m unittest discover -s tests -v        # 回归测试（14 项）
python3 tmp/check_frontend.py                   # 前端结构自洽性（HTML/JS/CSS 对齐）
```

`tmp/verify_e2e.py` 会真正启动服务、走完整流程（依次录入 → 分组批量导入 →
跨组查重 → 重开 → 导出），把两类项目的 docx / pdf / xls / 上传表都生成并逐项检查，
PDF 还会渲染成 PNG 供人工核对。

界面本身建议人工点一遍（本轮受限环境里浏览器跑不起来，只做了静态核对）：
重点看第 1 步「导入到分组」、第 2 步的分组表头行和「身份组」下拉。
