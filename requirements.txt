# RaPay 运行依赖
#
# 一般不需要手动装：启动 run_simple.py / run.py 时，bootstrap.py 会自动把
# 缺的包装进 vendor/pylib(2)，仓库里也已经带好了这些包。
#
# 想装到自己的环境里（或 vendor 目录损坏时重建）：
#     pip install -r requirements.txt
#     # 或者装回项目自带目录：
#     pip install --target vendor/pylib2 -r requirements.txt
#
# 版本与 vendor/ 中自带的一致，改动请同步 bootstrap.py 的 DEPENDENCIES。

python-docx==1.2.0      # 生成 Word 明细表
xlrd==2.0.2             # 读取上传的 .xls 名单
xlwt==1.3.0             # 导出 .xls 名单
openpyxl==3.1.5         # 读取上传的 .xlsx 名单
reportlab==5.0.1        # 生成 PDF（pdf_gen.py，不需要装 Office）
